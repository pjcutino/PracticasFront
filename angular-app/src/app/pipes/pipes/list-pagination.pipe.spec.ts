import { ListPaginationPipe } from './list-pagination.pipe';

describe('ListPaginationPipe', () => {
  it('create an instance', () => {
    const pipe = new ListPaginationPipe();
    expect(pipe).toBeTruthy();
  });
});
